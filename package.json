{
  "name": "codestudio-library-registry-generator",
  "version": "1.0.0",
  "description": "A standalone script to generate an enriched Arduino library registry from the official index and GitHub API.",
  "main": "generate.js",
  "scripts": {
    "start": "node generate.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "arduino",
    "library",
    "registry",
    "generator",
    "github-actions"
  ],
  "author": "CodeStudio Team",
  "license": "MIT",
  "dependencies": {
    "node-fetch": "^3.3.2"
  }
}
